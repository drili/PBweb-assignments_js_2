/*
1.0
Create an object Rational. It must have two properties, numerator, and denominator. The creating code must give values to these two properties.
*/
var Rational1 = {
      numerator: 7,
      denominator: 8
};

console.log(Rational1);
